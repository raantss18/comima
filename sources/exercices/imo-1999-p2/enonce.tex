Trouver la plus petite constante $C$ telle que, pour tout entier $n > 1$, l'inégalité
\[ \sum_{1 \le i < j \le n} x_i x_j (x_i^2 + x_j^2) \le C \left( \sum_{1 \le i \le n} x_i \right)^4 \]
soit vérifiée pour tous les réels $x_1, \dots, x_n \ge 0$. Déterminer les cas d'égalité.
