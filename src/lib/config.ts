/**
 * Configuration des fonctions « données » du site (compteur de visites et
 * bouton « utile 👍 » sur les exercices).
 *
 * Le site est statique : ces fonctions s'appuient sur un petit Google Apps
 * Script relié à une feuille Google (voir docs/google-apps-script.md).
 *
 * Tant que DATA_API_URL est vide, ces fonctions sont désactivées (aucune
 * requête réseau, aucun affichage). Collez ici l'URL « /exec » de votre
 * déploiement Apps Script pour les activer, puis publiez.
 */
export const DATA_API_URL = '';
